@livewire('customer-selection-table', [
    'context' => $context,
    'visibleColumns' => $visibleColumns,
    'availableFilters' => $availableFilters,
    'requirePhone' => $requirePhone,
    'requireEmail' => $requireEmail,
])
